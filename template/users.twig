{% extends "index.twig" %}

{% block header %}
    <div class="header">
        <div class="header__buttons">
            <button class="header__btn" data-action="login">login</button>
            <button class="header__btn" data-action="register">register</button>
        </div>
        <button class="header__order"></button>
    </div>
{% endblock %}

{% block content %}
    <div class="user">
        <ul class="user__list">
            {% for people in context %}
                <li class="user__item"
                        {% if people.age < 18 %}
                            style="background: #ffdbec"
                        {% elseif people.id == 1 %}
                            style="background: #dfdfdf"
                        {% else %}
                            style="background: #dce9ff"
                        {% endif %}
                >
                    <div class="user__idx">id:
                        <span class="user__id"> {{ people.id }} </span>
                    </div>
                    <div class="user__img">
                        <div class="user__pic"
                             style="background-image:url('/images/avatars/{{ people.avatar }}');"></div>
                    </div>
                    <h2 class="user__name"> {{ people.name }}
                        <small class="user__age">({{ people.age }} лет)
                            {% if people.age < 18 %}
                                <span class="user__minor">- несовершеннолетний</span>
                            {% endif %}
                        </small>
                    </h2>
                    <p class="user__desc"> {{ people.desc }} </p>
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}